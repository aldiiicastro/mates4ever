import {StyleSheet} from "react-native"

const createPetScreen = StyleSheet.create({
    viewStyle: {
        flex: 1,
        alignItems: 'center',
        backgroundColor: '#fff'
    }
})

export {createPetScreen}
