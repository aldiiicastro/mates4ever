import {StyleSheet} from "react-native";

const imageViewStyle= StyleSheet.create({
    imageView: {
        alignItems: 'center'
    },
    imageStyle: {
        width: '65%',
        height: 250,
        resizeMode: 'contain',
        margin: 15,
        borderRadius: 20,
    },
})

export {imageViewStyle}
