import {StyleSheet} from "react-native";

const buttonStyles = StyleSheet.create({
    buttonStyleContainer: {
        flex: 1,
        flexDirection: 'row',
        marginHorizontal: 90,
        marginTop: 5,
    },
    buttonStyle: {
        alignItems: "center",
        backgroundColor: "rgb(171,72,72)",
        borderRadius: 1100,
        padding: 10,
    }
})

export {
    buttonStyles
}